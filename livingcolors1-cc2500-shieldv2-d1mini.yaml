# These substitutions allow the end user to override certain values
substitutions:
  name: "livingcolors1"
  friendly_name: "LivingColors"

packages:
  core: !include d1mini.yaml
  # CC2500 Shield V2.0 on Wemos D1 mini
  cc2500: github://rrooggiieerr/esphome-cc2500/cc2500-shieldv2-d1mini.yaml@master
  livingcolors1: !include livingcolors1.yaml

esphome:
  name: "${name}"
  friendly_name: "${friendly_name}"
  comment: "Philips LivingColors 1st generation"
  # Automatically add the mac address to the name
  # so you can use a single firmware for all devices
  name_add_mac_suffix: true